#include<stdio.h>
# define PI 3.14
int main()
{
	int firstnumber=10;
	printf("The firstnumber is %d\n",firstnumber);
	printf("The value of PI is %.2f\n",PI);
	
	return 0;
}

/*
variable= that stores data
rules:
cannot start with numbers,symbols

constant= once declared cannot be
changed

suntax :
datatype var_name=value;
 constant can be defined after the #include<stdio.h>
 syntax:
 # define name value

*/
// space between printf is not correct
//there should not be space between variable
